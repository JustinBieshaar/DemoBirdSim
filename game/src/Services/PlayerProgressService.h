#pragma once
#include "IPlayerProgressService.h"

class PlayerProgressService : public IPlayerProgressService
{
	// empty just to showcase a transient example.
	// as player progress is a 'per-operation' service.
};